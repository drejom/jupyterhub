name: base
channels:
  - conda-forge
dependencies:
  - python=3.11
  - jupyterhub=4.0.2 
  - pip

# Pip packages
  - pip:
    - jupyterhub-nativeauthenticator
    - dockerspawner
    - wrapspawner
    - batchspawner
    - jupyterhub-idle-culler
